_schema-version: "3.2"
ID: actions
description: Generated by Fiori Tools
version: 0.0.2
modules:
- name: actions-destination-content
  type: com.sap.application.content
  requires:
  - name: actions-destination-service
    parameters:
      content-target: true
  - name: actions-repo-host
    parameters:
      service-key:
        name: actions-repo-host-key
  - name: actions-uaa
    parameters:
      service-key:
        name: actions-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: actions_html_repo_host
          ServiceInstanceName: actions-html5-service
          ServiceKeyName: actions-repo-host-key
          sap.cloud.service: actions
        - Authentication: OAuth2UserTokenExchange
          Name: actions_uaa
          ServiceInstanceName: actions-xsuaa-service
          ServiceKeyName: actions-uaa-key
          sap.cloud.service: actions
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: actions-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: actions-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - actions.zip
      name: actions
      target-path: resources/
- name: actions
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: actions-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: actions-destination-service
    service-plan: lite
- name: actions-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: actions-xsuaa-service
    service-plan: application
- name: actions-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: actions-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
